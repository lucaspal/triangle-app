import React from 'react';
import Note from './Note'

function Header() {
    return (
        < Note icon="ts-icon-followed" text="Fill in the length of each side to see which type of triangle it is." />
    )
}

export default Header;